# docker-compose.yaml
version: '3'

services:
  h3o-inferno:
    build: .
    command: ["python3", "app.py"]
    ports:
      - "1025:1025"
    volumes:
      - ./data:/app/data
    environment:
      FLASK_APP: app
      SQLITE_LOCATION: /app/data
